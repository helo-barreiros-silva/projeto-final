algoritmo "area e perimetro do quadrado"

var

lado, area, per: real

inicio

escreval("Digite o tamanho da lado: ")
leia(lado)


area <- lado^2
per <- lado*4
escreval("A área do quadrado é: ", area)
escreval("O perímetro do quadrado é: ", per)

fimalgoritmo